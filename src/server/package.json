{
  "name": "server",
  "version": "1.0.0",
  "description": "server that receives an image with bounding boxes and a message ",
  "main": "server.js",
  "dependencies": {
    "ws": "^8.4.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC"
}
